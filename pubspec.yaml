name: utility_collector
description: "Сбор данных счётчиков"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  share_plus: ^12.0.1
  flutter_platform_alert: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/electricity_meter.jpeg
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
